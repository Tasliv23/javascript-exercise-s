1. add_gigasecond;

Add 1 gigasecond to a date then outpu the result in date format.

2.binary_to_decimal

convert a binary number into decimal number.( the input is a string so it must have apostrophe's)

3.create_acronym

Create an acronym from a string.(input ="International Business Machines" output = IBM

4. letter_duplicate

check if a word has dupicate letter. 

5.word_count

count the occurrence of the words in a string.